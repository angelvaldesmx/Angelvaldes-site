[build]
  functions = "netlify/functions"
  publish = "dist"  # O "public", o tu carpeta de salida si usas algún generador

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
